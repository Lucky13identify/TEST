/*
 * Операторы сравнения
 */

// console.log(15 < 3);

/*
 * Приведение (преобразование) типов к числу
 */
// console.log('10' > 5);

// Идет привидение к значению по методу .charCodeAt(index) и через него сравнивает. Сравнивает по первому символу (индексу).
// console.log('10' > '5');

/*
 * Операторы равенства (== и ===) и неравества (!= и !==)
 * ✅ «ВСЕГДА ИСПОЛЬЗУЕМ СТРОГОЕ РАВЕНСТВО (===) и НЕРАВЕНСТВО (!==)»
 */

const isEqual = 1 === true;

console.log(isEqual);
